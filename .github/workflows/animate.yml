<line x1="458" y1="150" x2="792" y2="200"/>
              <line x1="458" y1="230" x2="792" y2="280"/>
              <line x1="458" y1="310" x2="792" y2="200"/>
            </g>
            
            <!-- Title -->
            <text x="50%" y="50" text-anchor="middle" fill="#00ff88" font-size="36" font-weight="bold" 
                  font-family="'Courier New', monospace">
              NEURAL INTELLIGENCE
            </text>
            
            <!-- Subtitle -->
            <text x="50%" y="380" text-anchor="middle" fill="#0099ff" font-size="16" 
                  font-family="'Courier New', monospace" opacity="0.8">
              → Learning • Adapting • Evolving ←
            </text>
          </svg>
          EOF
      - name: Deploy to output branch
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: gh-pages
